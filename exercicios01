#include <stdio.h>
struct node{
  int data;
  struct node* next;
};

int isEmpty(struct node* r){
  if(r != NULL){
    return 0;
  }
  else{
    return 1;
  }
}

int main() {
  struct node* head;
  head->data = 5;
  printf("%d", isEmpty(head));
  return 0;
}
